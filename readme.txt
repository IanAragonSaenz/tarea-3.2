Lib contians the file in order for antlr to work.

bin contains files that makes compiling and running antlr easier.

For antlr some changes to path are required. 

The commands fro antlr to be used are attached in an image.

PLY runs by just running "python parser.py test.txt".